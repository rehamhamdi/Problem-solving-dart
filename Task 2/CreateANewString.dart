void main(){
  var stdin;
  String ? S=stdin.readLineSync();
      String ? T=stdin.readLineSync();
      
      print('${S?.split('').length}  ${T?.split('').length}');
      print('$S $T');

}